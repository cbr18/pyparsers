# Анализ парсинга image_gallery и first_registration_time

## 📊 Результаты анализа логов (за 24 часа)

### DONGCHEDI:

1. **image_gallery**:
   - ✅ Найдено: **469 раз**
   - ❌ Не найдено: **305 раз**
   - **Статус**: ✅ **ПАРСИТСЯ** (но не всегда успешно)

2. **first_registration_time**:
   - ✅ Найдено: **0 раз**
   - ❌ Не найдено: **305 раз**
   - **Статус**: ❌ **НЕ ПАРСИТСЯ**

3. **Блокировки**:
   - Заблокировано: **158 раз**
   - Mobile fallback: успешно **0 раз**, ошибок **610 раз**

### CHE168:

1. **image_gallery**:
   - ✅ Найдено: **0 раз** (не логируется или не парсится)
   - **Статус**: ❌ **НЕ ПАРСИТСЯ** или не логируется

2. **first_registration_time**:
   - ✅ Найдено: **731 раз**
   - **Статус**: ✅ **ПАРСИТСЯ** успешно

3. **API блокировки**:
   - 403 ошибки: **400+ раз**
   - Fallback успешно: **81 раз**

---

## 🔍 Как парсятся эти поля?

### DONGCHEDI - image_gallery:

**Метод 1: Из __NEXT_DATA__ (основной)**
- Извлекается из `props.pageProps.skuDetail.head_images`
- Парсится через Selenium на странице
- **Работает**: ✅ (469 раз найдено)

**Метод 2: Мобильная версия (fallback)**
- URL: `https://m.dongchedi.com/usedcar/{car_id}`
- Парсинг через requests + BeautifulSoup
- Извлекает `head_images` из `__NEXT_DATA__`
- **Работает**: ❌ (0 успешных, 610 ошибок)

**Метод 3: HTML fallback**
- Парсинг через CSS селекторы
- Ищет изображения в HTML
- **Работает**: частично (не находит изображения)

### DONGCHEDI - first_registration_time:

**Метод 1: Из __NEXT_DATA__ (основной)**
- Извлекается из `props.pageProps.skuDetail.other_params`
- Ищет параметры: `'上牌时间', '首次上牌', '首次上牌时间'`
- **НЕ РАБОТАЕТ**: ❌ (0 раз найдено)

**Метод 2: Мобильная версия (fallback)**
- Парсинг через requests
- Извлекает из `other_params` в `skuDetail`
- **НЕ РАБОТАЕТ**: ❌ (0 успешных)

**Метод 3: HTML fallback**
- Парсинг через паттерны в тексте страницы
- Ищет даты в формате YYYY-MM-DD
- **НЕ РАБОТАЕТ**: ❌ (0 раз найдено)

### CHE168 - image_gallery:

**Метод 1: API getcarinfo**
- Ищет в полях: `piclist`, `head_images`, `images`, `picurl`
- **НЕ РАБОТАЕТ**: ❌ (API блокируется 403)

**Метод 2: Selenium fallback**
- Парсит `head_images` из `__NEXT_DATA__`
- URL: `https://m.che168.com/cardetail/index?infoid={car_id}`
- **Работает**: частично (81 раз через fallback)
- **Но не логируется** когда находит галерею!

**Метод 3: Selenium parser.py**
- Метод `fetch_car_detail()` в `parser.py`
- Парсит `head_images` из `skuDetail`
- **Работает**: ✅ (но не используется в detailed парсере)

### CHE168 - first_registration_time:

**Метод 1: API getcarinfo**
- Извлекается из поля `regdate`
- Нормализуется через `normalize_first_registration_date()`
- **Работает**: ✅ (731 раз найдено)

**Метод 2: Selenium parser.py**
- Парсится из HTML или `skuDetail`
- **Работает**: ✅ (но не всегда)

---

## ❌ Почему перестали парситься?

### DONGCHEDI - first_registration_time:

**Причины**:
1. **Блокировка Selenium** - `__NEXT_DATA__` не загружается (158 блокировок)
2. **Мобильная версия не работает** - 610 ошибок fallback
3. **Данные не в other_params** - возможно структура изменилась
4. **HTML fallback не находит** - паттерны не соответствуют

**Временные блокировки**:
- Selenium часто блокируется
- Мобильная версия тоже блокируется (статус 514)
- Возможно, сайт детектирует автоматизацию

### CHE168 - image_gallery:

**Причины**:
1. **API блокируется** - 400+ ошибок 403
2. **Fallback работает**, но не логируется
3. **Данные не возвращаются** API даже когда работает

**Временные блокировки**:
- API getcarinfo часто возвращает 403
- Но fallback работает (81 успешных)
- Проблема в том, что галерея не логируется при успешном fallback

---

## 💡 Что можно сделать?

### Для DONGCHEDI - first_registration_time:

1. **Улучшить мобильный fallback**:
   - Проверить, почему мобильная версия не работает
   - Улучшить обработку ошибок
   - Добавить больше заголовков

2. **Улучшить HTML fallback**:
   - Добавить больше паттернов поиска даты
   - Проверить другие места в HTML

3. **Использовать другие источники**:
   - Проверить другие поля в `skuDetail`
   - Использовать альтернативные API endpoints

4. **Обработка временных блокировок**:
   - Добавить задержки между запросами
   - Ротация User-Agent
   - Использовать прокси

### Для CHE168 - image_gallery:

1. **Улучшить логирование**:
   - Логировать когда fallback находит галерею
   - Добавить больше информации в логи

2. **Использовать parser.py**:
   - Вызвать `Che168Parser.fetch_car_detail()` в detailed парсере
   - Он уже работает и находит галерею

3. **Улучшить fallback**:
   - Использовать тот же подход, что в parser.py
   - Увеличить время ожидания
   - Улучшить парсинг __NEXT_DATA__

4. **Обработка временных блокировок**:
   - Добавить задержки между запросами
   - Ротация заголовков
   - Использовать разные методы парсинга

---

## 🕐 Есть ли временные блокировки?

### DONGCHEDI:

- **Selenium часто блокируется** (158 раз)
- **Мобильная версия блокируется** (514 ошибок)
- Возможно, есть паттерны блокировок по времени
- Рекомендуется: добавить задержки и ротацию

### CHE168:

- **API часто блокируется** (400+ ошибок 403)
- **Но fallback работает** (81 успешных)
- Блокировки не постоянные, но частые
- Рекомендуется: использовать fallback как основной метод

---

## 📋 Итоговые рекомендации:

1. **DONGCHEDI first_registration_time**:
   - Проверить структуру данных в `other_params`
   - Улучшить мобильный fallback
   - Добавить больше источников данных

2. **CHE168 image_gallery**:
   - Использовать parser.py метод напрямую
   - Улучшить логирование fallback
   - Использовать fallback как основной метод

3. **Общие рекомендации**:
   - Добавить задержки между запросами
   - Ротация User-Agent и заголовков
   - Мониторинг блокировок
   - Использовать несколько методов парсинга









